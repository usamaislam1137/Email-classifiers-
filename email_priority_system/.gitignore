# Python
ml/venv/
**/__pycache__/
**/*.py[cod]
**/.Python
ml/.pytest_cache/
ml/hf_cache/

# Rails
rails_app/tmp/
rails_app/log/
rails_app/storage/
rails_app/.bundle/
rails_app/vendor/bundle/
rails_app/db/*.sqlite3
rails_app/db/*.sqlite3-*

# Secrets (never commit)
rails_app/tmp/local_secret.txt
.env
.env.*

# Logs
*.log
ml/pipeline.log

# ML: keep default datasets + trained artefacts in-repo for clone-and-run (Docker mounts these paths).
# Re-add ignores here if raw corpora grow too large for Git.

# OS / IDE
.DS_Store
.idea/
*.swp
