# Slim deps for the Flask inference image only (Docker).
# Drops PyTorch + Hugging Face stack (~GB+) so builds fit typical Docker Desktop disks.
# Training / BERT: use requirements.txt on the host or a dedicated training image.
#
# Inference uses use_bert=False (see predict.py); RF/XGB/LR models match no-BERT features.

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
shap>=0.43.0
nltk>=3.8.0
flask>=3.0.0
flask-cors>=4.0.0
imbalanced-learn>=0.11.0
joblib>=1.3.0
requests>=2.31.0
tqdm>=4.66.0
scipy>=1.11.0
email-validator>=2.1.0
python-dateutil>=2.8.0
chardet>=5.2.0
